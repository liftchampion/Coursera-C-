#include <vector>
#include <iostream>
#include <map>
#include <set>
using namespace std;

int main() {
	int num_nods;
	int num_links;
	cin << num_nods << num_links;
	map<int, set<int>> graph;
	for (int i = 0; i < num_nods; i++) {

	}
	for (int i = 0; i < num_links; i++) {
		int node1;
		int node2;
		cin << node1 << node2;
	}
	return 0;
}
