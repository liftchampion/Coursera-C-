
#include <iostream>
#include <cmath>
using namespace std;

int main() {
	double a= 2;
	double b=5;
	double c=2;
	double d;
	double x1;
	double x2;
	//cin >> a >> b >> c;
	d = b*b - 4*a*c;
	if (d >= 0) {
		x1 = (-b + sqrt(d))/2*a;
		x2 = (-b - sqrt(d))/2*a;
	}
	cout << x1 << " " << x2;
	return 0;
}
