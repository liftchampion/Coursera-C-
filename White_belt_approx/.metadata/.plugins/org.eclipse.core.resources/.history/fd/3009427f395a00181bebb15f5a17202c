#include <vector>
#include <string>
#include <iostream>
#include <map>
using namespace std;

class TreeNode {
public:
	int AddWord(const string& word) {
		value = word[0];
		word.erase(word.begin());
	}
private:
	char value;
	vector<TreeNode> children;
};

int main() {
	string word;
	cin >> word;
	return 0;
}
