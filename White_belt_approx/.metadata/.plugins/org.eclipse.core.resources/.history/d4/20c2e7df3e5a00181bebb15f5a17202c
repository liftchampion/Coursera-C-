#include <vector>
#include <string>
#include <iostream>
#include <map>
using namespace std;



class TreeNode {
public:
	TreeNode () {
		value = '#';
	}
	void AddWord(string& word) {

		value = word[0];
		word.erase(word.begin());
		if (word.size() > 0) {
			children[word[0]].AddWord(word);
		}

	}
private:
	char value;
	map<char, TreeNode> children;
};


class Forest {
public:
	void AddWord(string& word) {
		//return trees[word[0]].AddWord(word);
	}
private:
	map<char, TreeNode> trees;
};



int main() {
	string word;
	cin >> word;
	return 0;
}
