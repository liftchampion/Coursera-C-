#include <string>
#include <vector>
using namespace std;

#define H(a) #a

#define H2(a) H(a)

#define UNIQ_ID H2(var ## __LINE__)

int main() {
  int UNIQ_ID = 0;
  string UNIQ_ID = "hello";
  vector<string> UNIQ_ID = {"hello", "world"};
  vector<int> UNIQ_ID = {1, 2, 3, 4};
}
