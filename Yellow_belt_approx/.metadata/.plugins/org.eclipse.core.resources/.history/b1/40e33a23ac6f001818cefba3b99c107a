#include <vector>
#include <iostream>
#include <deque>
#include <map>
using namespace std;

void GetTheWorst(int n) {
	deque<int> nums;
	map<int, int> result;
	for (int i = 1; i <= n; i++) {
		nums.push_back(i);
	}
	while (nums.size() > 0) {
		int current = nums.pop_back();
	}
}




int main() {
	unsigned n;
	cin >> n;
	vector<int> nums(n);
	for (unsigned i = 1; i <= n; i++) {
		nums[i-1] = i;
	}
	vector<int> sequence(n);


	return 0;
}
